<div class="container-xl-12">
  <div class="row">
    <h2 class="mx-auto follow-name"><%= @user.name %>さんのフォロー一覧</h2>
    <div class="col-md-12 user-index">
      <% if @follows.empty? %>
        <h1 class="not-user">ユーザーがいません</h1>
      <% else %>
        <div class="scroll-list jscroll">
          <% @follows.each do |following| %>
            <ul class="align-items-center center2">
              <li class="follow-catalog">
                <%= link_to user_path(following.id) do %>
                  <%= attachment_image_tag following, :image, fallback: "no_image.jpg", class: "text-center img2" %>
                <% end %>
              </li>
              <li class="follow-catalog abridgement4">
                名前：
                <%= link_to following.name, user_path(following.id) %>
              </li>
              <li class="follow-catalog">
                dog：
                <%= following.dogs.count %>匹
              </li>
              <li class="follow-catalog">
                投稿：
                <%= following.articles.count %>本
              </li>
            </ul>
          <% end %>
          <%= paginate @follows %>
        </div>
      <% end %>
    </div>
  </div>
</div>
