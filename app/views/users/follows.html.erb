<div class="container-xl-12">
  <div class="row">
    <div class="col-md-12 user-index">
      <h2>フォロー一覧</h2>
      <% if @follows.empty? %>
        <%= "ユーザーはいません" %>
      <% else %>
        <div class="scroll-list jscroll">
          <% @follows.each do |following| %>
            <ul class="justify-content-around align-items-center">
              <li>
                <%= link_to user_path(following.id) do %>
                  <%= attachment_image_tag following, :image, :fill, 60, 60, fallback: "no_image.jpg", size:'100x100' %>
                <% end %>
              </li>
              <li>
                名前：
                <%= link_to following.name, user_path(following.id) %>
              </li>
              <li>
                dog数：
                <%= following.dogs.count %>匹
              </li>
              <li>
                投稿数：
                <%= following.articles.count %>
              </li>
            </ul>
          <% end %>
          <%= paginate @follows %>
        </div>
      <% end %>
    </div>
  </div>
</div>
